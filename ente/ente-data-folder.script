#!/bin/bash

ENTE_DATA_FOLDER="$HOME/ente/data"

if [[ -d "$ENTE_DATA_FOLDER" ]]; then
    exit
else
    mkdir -p "$ENTE_DATA_FOLDER"
    # This feedback message is the only reason for this not just being a oneliner.
    echo "$ENTE_DATA_FOLDER created."
fi
