#!/bin/bash

ADGUARD_CONF_FOLDERS=("$HOME/adguard/work" "$HOME/adguard/conf")

for folder in "${ADGUARD_CONF_FOLDERS[@]}"; do
    if [[ -d "$folder" ]]; then
        exit
    else
        mkdir -p "$folder"
        echo "$folder created."
    fi
done
